You will need to have PyTorch, sklearn, matplotlib, pandas, numpy, scipy installed

The source code is in `actpred`.

To install this `cd` into `actpred` and run `pip install .`

The notebook `Train_And_Use_ADHunter.ipynb` walks through how to featurize 40aa sequences, how to train the model, and how to evaluate the model on test data. 
